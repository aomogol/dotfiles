[pull]
	rebase = false
[push]
	default = simple
[user]
	name = aomogol
	email = aomogol@gmail.com
[credential]
	helper = cache --timeout=32000
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
